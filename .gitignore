# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# The tracker's write lock. An empty file whose only content is the OS lock
# held on it, recreated on demand - committing it would be committing nothing
# and conflicting over it.
.issues/.lock
