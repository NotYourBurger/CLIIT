<!--
Keep this current as you work:
- mark each checkpoint complete immediately after completing it
- record important discoveries or decisions as they happen
- keep the Current and Next fields accurate
- do not wait until the end of the session to record progress
-->

# ISS-034 Work Plan

## Goal

Reopening an issue must drop the resolution it no longer has

## Plan

- [x] Trace `set_fields` validation/write ordering and the existing close/reopen test fixture.
- [x] Clear resolution fields only when a closed issue is reopened, preserving legacy absent fields.
- [x] Extend `test_close.py` through reopen, legacy reopen, and a fresh second close.
- [x] Run focused and full verification and review the finished change.

## Decisions

- Put cleanup in `set_fields` immediately before the write, after validation, so all rejected updates remain non-mutating.

## Discoveries

- Existing close coverage deliberately retained the old resolution after `set ... open`; that assertion captures the reported bug and must be reversed.
- Both rendered and JSON views derive their Resolution block from the flat fields, so removing the fields at the write makes every reader consistent without adding a second opinion.
- The evidence-closing PRD still specified preserving resolution fields on reopen; it now records git/`issue log` as the history mechanism, matching the repository's established audit model.

## Current

Closed as completed with implementation commit `8994deb` and focused/full test evidence.

## Next

None.
