<!--
Keep this current as you work:
- mark each checkpoint complete immediately after completing it
- record important discoveries or decisions as they happen
- keep the Current and Next fields accurate
- do not wait until the end of the session to record progress
-->

# ISS-058 Work Plan

## Goal

Release 0.1.1 with a fixed hero, install line and terminal demo

## Plan

- [x] Sharpen the README hero (title line + subhead), leave the install line alone
- [x] Write `docs/demo.sh` - the exact scripted session, runnable and deterministic
- [x] Record it and render a GIF; commit both and link above the fold
- [x] Bump to 0.1.1 with a CHANGELOG entry, `uv lock`, full suite
- [x] Hand the tag push to the maintainer and verify the PyPI page after

## Decisions

- One issue, not three: the hero is what the release carries to PyPI, so it has
  to land before the tag or 0.1.2 exists only to fix copy.
- Demo recorder: no vhs/asciinema/ffmpeg on this machine and Windows has no pty,
  but WSL Ubuntu has python3 + uv. Record with asciinema in WSL, render with a
  prebuilt `agg` binary - both user-local, no apt, no sudo.
- The `.cast` is committed next to the GIF: it is text, it diffs, and it is the
  thing that makes the GIF reproducible. Same reason `.issues/` is Markdown.

## Discoveries

- The README image had to be an absolute raw.githubusercontent URL. PyPI
  renders the README with no repository context, so `docs/demo.gif` would have
  been a broken image on exactly the page this release exists to fix.
- `git` opens a pager on a tty and clears the screen when it leaves, which ate
  the closing diff out of the first recording. `GIT_PAGER=cat` in the script.
- Recorded from WSL: Windows has no pty, so asciinema cannot record here. `uv
  tool install asciinema` plus the prebuilt `agg` binary, both user-local.
- 26.6s, 243K. `uvx twine check` passes on both distributions.
- The img does survive PyPI's sanitizer: checked by running the published
  description through `readme_renderer.markdown`, since pypi.org serves curl a
  bot-challenge page rather than the project page.
- A clean `uv pip install cliit` got 0.1.0 for a few minutes after publishing -
  CDN lag between the JSON API and the simple index, not a bad release. Pinned
  and unpinned both give 0.1.1 now.

- The release is tag-driven and the `pypi` environment needs maintainer
  approval (`docs/RELEASING.md`), so the last two checkpoints are not mine to
  close - I prepare the commit, the maintainer pushes `v0.1.1`.

## Current

Done. 0.1.1 is on PyPI and the page is verified.

## Next

Nothing for this issue. Two things found on the way out, neither in scope here:

- `docs/RELEASING.md` says `git tag -s`, but there is no signing key on this
  machine and v0.1.0 is unsigned too (`%G?` is `N`). The doc and both releases
  disagree; pick one.
- README's links to `SECURITY.md` and `CONTRIBUTING.md` are relative, so they
  are broken on PyPI for the same reason the demo image would have been. Not
  introduced here - 0.1.0 shipped them - but it is the same defect.
