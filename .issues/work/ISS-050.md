<!--
Keep this current as you work:
- mark each checkpoint complete immediately after completing it
- record important discoveries or decisions as they happen
- keep the Current and Next fields accurate
- do not wait until the end of the session to record progress
-->

# ISS-050 Work Plan

## Goal

The README says what the commands are, not who it is for

## Plan

- [x] read the README's shape, decide where the header goes
- [x] write the header: what/who, thirty-second example, non-goals
- [x] verify every claim in it against the code (flags, ids, git)
- [x] run the suite and `issue check`

## Decisions

- The header goes above `## Install`, not replacing anything. The verb list is
  a good reference; the issue says so.

## Discoveries

- `issue init` in a repo with neither CLAUDE.md nor AGENTS.md writes no agent
  instructions and says so on stderr (ISS-044). The first draft of the example
  claimed it wrote the rule unconditionally; corrected to "if you keep one".
- `python -m cli_issue_tracker.cli` exits 0 doing nothing - there is no
  `__main__` guard, only the `issue` entry point. Walked the whole example
  through `.venv/Scripts/issue.exe` in a throwaway repo instead.
- `events.py` is missing from the Layout table. Not this issue; noted.

## Current

Suite green (20 files ok), `issue check` clean.

## Next

Close with evidence.
