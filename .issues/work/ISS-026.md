<!--
Keep this current as you work:
- mark each checkpoint complete immediately after completing it
- record important discoveries or decisions as they happen
- keep the Current and Next fields accurate
- do not wait until the end of the session to record progress
-->

# ISS-026 Work Plan

## Goal

issue start - the work plan replaces the session handover

## Plan

- [x] plan.py: the rule constant, the seed, and a reader that never raises
- [x] issue start - seeds, resumes, refuses a blocked issue
- [x] issue next - plan state and live git
- [x] issue view - the compact plan block
- [x] issue close - warn about unchecked checkpoints
- [x] issue init - write the workflow rule into CLAUDE.md / AGENTS.md
- [x] Delete handover.py, its four verbs and its test
- [x] README and CLAUDE.md brought back in sync

## Decisions

- join_sections is deleted rather than moved down: nothing writes a plan after
  `start` seeds it, so it would be dead code kept alive by its own test. The
  guard that replaces it is US-006's - reading never raises, unknown headings
  ignored, missing means absent.
- The plan block prints GIT above NEXT, not below as the PRD's example draws
  it. ISS-025 already settled that ordering and closed on it.
- The condensed reminder in the seed is literally RULE[4:8], so the two
  renderings cannot drift by hand.

## Discoveries

- The PRD's four condensed bullets are exactly lines 5-8 of the nine-line rule,
  so "one constant, two renderings" is mechanical rather than a promise.
- split_sections already drops everything above the first `## `, so the seed's
  HTML comment and title need no special case in the reader.
- Dogfooding this on its own plan found two bugs the tests had not: bullets
  that wrap were truncated at the first line, and `next` printed GOAL under
  a title line that already said it.
- next_id's `prefix` argument existed only so handovers could number H-NNN
  against their own directory. Plans are named after their issue, so the
  argument went with the feature.

## Current

Done. Twelve test files green, `issue --help` shows one new verb and four
fewer, and this file is what `issue start ISS-026` seeded on its own feature.

## Next

Commit, then close with the suite and this plan as evidence.
