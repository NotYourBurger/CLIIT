<!--
Keep this current as you work:
- mark each checkpoint complete immediately after completing it
- record important discoveries or decisions as they happen
- keep the Current and Next fields accurate
- do not wait until the end of the session to record progress
-->

# ISS-059 Work Plan

## Goal

Globally unique issue ids, with no coordination

## Plan

- [x] Trace allocation, ID validation, and affected compatibility tests
- [x] Generate sortable, offline-unique IDs while retaining exclusive reservation
- [x] Cover independent trees and legacy IDs; update affected existing checks
- [x] Correct the four stale coordination documents and run the suite

## Decisions

- Keep legacy numeric IDs valid; only newly generated IDs use the new form.
- Use existing command tests as numeric-ID compatibility fixtures; `test_id_scheme.py` tests new allocation directly.

## Discoveries

- `reserve_id()` is the sole allocation path; its O_EXCL reservation already protects a generated candidate.
- Creation output starts with "Issue", so tests take the generated ID from its second word.

## Current

Closed after the full suite and tracker check passed.

## Next

None.
