<!--
Keep this current as you work:
- mark each checkpoint complete immediately after completing it
- record important discoveries or decisions as they happen
- keep the Current and Next fields accurate
- do not wait until the end of the session to record progress
-->

# ISS-029 Work Plan

## Goal

issue close - --verified alone is not proof of completion

## Plan

- [x] tests/test_close.py: verified-only close warns on stderr, exits 0, stdout unchanged
- [x] tests/test_close.py: evidence with a commit or a test alongside warns about nothing
- [x] close_issue: the warning, after the write, beside the unticked-checkpoint one
- [x] tests/all.py green, untouched
- [x] README

## Decisions

- Option (a) from the issue: warn and close anyway. The issue asks for a
  decision, but its own acceptance criteria are written for (a) and the
  argument against (b) is in the issue - `tests/helpers.py:close()` closes with
  `--verified` on purpose, so the fixtures are not tied to whatever repo they
  run inside. That is a legitimate case for evidence a machine cannot check.

## Discoveries

- The guard has to be `completed and all(...)`: `all()` over the empty evidence
  list is True, so a plain `--not-planned` close would have picked up the
  warning. The suite now asserts that it does not.
- The warning goes after the write, not next to the validators that reject
  evidence. Before the write there are still three ways to exit 1 (unknown id,
  self-duplicate, dangling target), and a warning about a close that never
  happened is worse than no warning.

## Current

Done. Warning in close_issue, three cases in test_close.py, README updated,
suite green and `issue check` clean. Committed as 4ae4df6.

## Next

Close ISS-029 with the commit and the suite as evidence.
