<!--
Keep this current as you work:
- mark each checkpoint complete immediately after completing it
- record important discoveries or decisions as they happen
- keep the Current and Next fields accurate
- do not wait until the end of the session to record progress
-->

# ISS-037 Work Plan

## Goal

Compact work-plan output for repeated agent reads

## Plan

- [x] Measure existing output and specify a compatible compact view.
- [x] Implement shared bounded rendering and CLI flags.
- [x] Verify long active plans, CLI behavior, and preserved full output.
- [x] Update README and measure savings on stored plans.
- [x] Run the full suite and checker.

## Decisions

- Compact output is opt-in: the recovery report shows that full rationale helps
  fresh sessions resume. Existing plan files and JSON retain all content.
- Measure characters honestly; no model tokenizer is installed.

## Discoveries

- Ten stored plans average 1535.6 characters of resume content. The seed itself
  is only 373 characters for an example title; accumulated prose dominates.
- Compact plan content averages 249.5 characters on those same ten plans,
  down 83.8% (15356 to 2495 total). Command headers and paths are excluded.
- The long active-plan test preserves the original plan bytes and full JSON,
  includes three pending checkpoints and explicitly counts the other two.
- All 15 test scripts pass; issue check returns an empty findings array.

## Current

Complete. ISS-037 is closed with test evidence; changes are uncommitted.

## Next

No remaining implementation work for ISS-037. The broader audit findings are separate.
