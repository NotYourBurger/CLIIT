<!--
Keep this current as you work:
- mark each checkpoint complete immediately after completing it
- record important discoveries or decisions as they happen
- keep the Current and Next fields accurate
- do not wait until the end of the session to record progress
-->

# ISS-035 Work Plan

## Goal

Duplicate ids: name the ceiling on next_id, and catch the residue

## Plan

- [x] Add focused `issue check` regressions for duplicate ids and independent prefixes.
- [x] Detect duplicate parsed ids while preserving the existing checks and output contracts.
- [x] Put `convert_id.py`'s inline allocator self-check under `tests/all.py`.
- [x] Document `next_id`'s concurrent-worktree ceiling and upgrade path.
- [x] Run focused checks, the allocator check, the full suite, and `issue check`.
- [x] Commit the implementation and prepare ISS-035's close evidence.

## Decisions

- Keep duplicate detection in the existing directory pass where `by_id` is built; the later issue-wide checks should continue to operate on the same map.
- Preserve the first filename separately for diagnostics while leaving `by_id`'s established last-file-wins behavior unchanged; the check reports corruption without inventing a recovery policy.

## Discoveries

- `tests/all.py` already discovers every `test_*.py`, so a standalone `tests/test_convert_id.py` is enough to adopt the existing inline self-check without changing the runner.
- Duplicate fixtures can use deliberately different filenames because identity comes from frontmatter; this directly reproduces the post-conflict rename residue.
- The README's checker findings and test inventory both needed the new behavior recorded so the user-facing contract stays complete.
- The full 14-script suite passes, including the newly discovered allocator wrapper; the direct allocator self-check also passes and `issue check` remains silent on the live database.

## Current

Closed with implementation commit 43e6e64 and the full verification evidence recorded on ISS-035.

## Next

None — ISS-035 is closed.
