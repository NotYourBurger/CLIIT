---
id: ISS-047
status: closed
created_at: 2026-09-08T12:01:57+06:00
updated_at: 2026-09-08T19:51:30+06:00
priority: high
labels: internals, launch
assignee: tahmid
reason: completed
closed_at: 2026-09-08T19:51:30+06:00
message: One .github/workflows/ci.yml: the suite on ubuntu, macos and windows across Python 3.11, 3.12 and 3.13, plus a job that installs the built wheel into a clean environment and a job with git deleted off the machine. requires-python was widened to >=3.11 after measuring it rather than assuming it - the matrix the issue asks for would otherwise have failed six jobs on metadata alone. Running the no-git job's command locally found two failures, both in the tests' own git fixtures rather than in the tool, and both fixed here: a job red by construction is a broken job, not a finding. The findings the matrix itself produces - fcntl, case-sensitive file_by_id, a machine that is not cp1252 - arrive on the first push and get filed, per the acceptance.
evidence: [{"type": "commit", "value": "ec171df"}, {"type": "test", "value": "uv run python tests/all.py - 21/21 ok"}, {"type": "test", "value": "the same suite with git removed from PATH - 21/21 ok, test_log skipping"}, {"type": "test", "value": "the whole suite on a real CPython 3.11.15 interpreter - 21/21 ok, which is what widened requires-python"}, {"type": "verified", "value": "built the wheel, installed it into a venv that had never seen the checkout, and ran the job's exact sequence outside any source tree: --version, init, create, list --json, next, all exit 0"}, {"type": "verified", "value": "uv run issue check exits 0; issue check --plans reports ISS-047 touched, 7/7 ticked"}]
---

# No CI: every POSIX path in this tool has never run

Every test run this project has had was on one Windows machine on one
interpreter. That makes several claims in CLAUDE.md unverified rather than
false:

- `storage.locked` forks on platform. `msvcrt.locking(LK_LOCK)` is mandatory
  and raises after about ten seconds; `fcntl.flock` is advisory and blocks
  forever. Same intent, different semantics, and the `fcntl` branch has never
  executed.
- `test_degraded.py` asserts the six `run_git` paths degrade rather than
  fail, but never against a machine that actually has no git installed.
- `file_by_id` resolves `ISS-001.md` and `iss-001.md` to the same file on
  Windows and to different files on Linux.
- the UTF-8 and LF pinning exists because this machine defaults to cp1252 and
  CRLF. Nothing checks the behaviour on a machine that defaults the other way.

None of these is known to be broken. All of them are unmeasured, and CI is
the cheapest thing that turns the whole list into facts.

## Scope

One `.github/workflows/ci.yml`. `tests/all.py` already exits non-zero on
failure, so this needs no test framework and the no-framework policy stands
untouched.

Matrix: ubuntu, macos and windows against 3.11, 3.12 and 3.13. Plus one job
that installs the built wheel rather than running from the source tree, and
one with git removed from the path.

## Acceptance

- the matrix runs on push and on pull request
- the wheel job proves the README install line is true on a clean machine
- whatever fails is filed rather than fixed inside this issue
