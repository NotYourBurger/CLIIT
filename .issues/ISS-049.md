---
id: ISS-049
status: closed
created_at: 2026-09-08T12:02:17+06:00
updated_at: 2026-09-09T00:40:05+06:00
priority: medium
labels: bug, internals
assignee: tahmid
reason: completed
closed_at: 2026-09-09T00:40:05+06:00
message: Two checks read this repo's own .issues/ - test_check pointed ISSUES_DIR at it, test_storage copied ISS-003.md - so whether the suite passed depended on which issues were filed. tests/fixtures/ is that corpus now, frozen and committed: ISS-003 verbatim for the pre-updated_at shape, one live issue carrying every optional field plus an unknown frontmatter key and a body with a bare --- rule, a blockquote and a fence, and one closed issue whose evidence line is JSON. The suite asserts the directory is non-empty first, because an empty ISSUES_DIR passes check while proving nothing. No opt-in script was written for the real .issues/ - uv run issue check already is one - so CI got the step instead, where the repository state under test is the branch that changed it. check --plans left the suite for good: it reports how well the plans are kept, a fact about the people, and check_plans is already covered three items up.
evidence: [{"type": "commit", "value": "04e86fd"}, {"type": "commit", "value": "2c73c2d"}, {"type": "test", "value": "uv run python tests/all.py - 21/21 ok"}, {"type": "verified", "value": "cloned the repo, deleted .issues entirely, ran issue init and created two unrelated issues, ran tests/all.py there - 21/21 ok on a tracker where ISS-003 does not exist"}, {"type": "verified", "value": "broke split_file in that clone so the fence search finds the last --- instead of the first; test_check now fails on the fixtures with 'ISS-004.md: would not survive a rewrite' and test_storage on 'body did not survive the round trip'"}, {"type": "verified", "value": "ISSUES_DIR=tests/fixtures uv run issue check exits 0; uv run issue check on the real .issues/ exits 0"}]
---

# The suite fails on a clone whose .issues differs from this one

Nineteen of the twenty-one test files isolate into a `TemporaryDirectory`
with `ISSUES_DIR` set, so the suite does not touch a user's tracker. Two do
not, on purpose:

- `tests/test_check.py:453` points `ISSUES_DIR` at the repo's real
  `.issues/` and asserts `check` exits clean. The comment gives a good
  reason - "the day a parser change starts eating prose it fails here rather
  than in an issue nobody reopens for a month."
- `tests/test_storage.py:83` copies the real `.issues/ISS-003.md` as its
  round-trip fixture.

Both are reads, so nothing is corrupted and no one's issues are rewritten.
But both make the suite's result depend on repository state rather than on
the code. A contributor who files an issue while working - which this repo's
own workflow tells them to do - can fail a test that has nothing to do with
their change, and a clone where ISS-003 has been renamed or closed
differently fails outright.

## Scope

Keep the intent and drop the coupling: a small committed corpus under
`tests/fixtures/` that both the parser round trip and the whole-directory
check run against, with ISS-003's body carried in as one of them.

Whether the real `.issues/` should still be checked as well, as a separate
opt-in script rather than part of `all.py`, is worth deciding in the same
pass.

## Acceptance

- `uv run python tests/all.py` passes on a clone with an arbitrary `.issues/`
- the nasty-body round trip still runs against content equivalent to ISS-003
- a parser change that eats prose still fails the suite
